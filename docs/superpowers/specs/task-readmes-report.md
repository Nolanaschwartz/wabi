# README cleanup task report

**Status:** Complete

All 9 edits applied across `packages/research/README.md`, `packages/web/README.md`, `packages/shared/README.md`, and `packages/bot/README.md`. Changes: `cron-compile/` promoted to its own bullet; `loadDotenv()` parenthetical reworded; trailing duplicate sentence removed; degraded-mode paragraph condensed; consent routes split to `accept/`+`decline/`; research proxy route and `admin/research/` UI added; `router` role added to shared; bot `src/lib/` helpers fully enumerated.
